## CH341AでT5810のBIOSを吸い出す手順（Windows 11）

---

### 必要なもの
- CH341AプログラマーとUSBケーブル
- SOIC8テストクリップ
- AsProgrammer v2.1.2（ソフト）
- T5810本体（電源OFF・ACアダプタ抜き）

---

### 1. AsProgrammer v2.1.2のインストール

1. 以下からZIPをダウンロード  
   → `https://github.com/nofeletru/UsbAsp-flash/releases`
2. ZIPを展開し、`AsProgrammer.exe` を任意フォルダに置く（インストール不要）

---

### 2. CH341Aドライバのインストール

1. CH341Aはまだ接続しない
2. `AsProgrammer_2.1.2\drivers\CH34X\CH34XPAR.EXE` を実行してインストール
3. インストール後、CH341AをUSBポートに挿す
4. `Win + X` → 「デバイスマネージャー」を開く
5. 「Interface」または「ポート (COMとLPT)」または「ユニバーサルシリアルバスコントローラー」を展開する
6. `USB EPP/I2C... CH341A` が表示されていることを確認
   - 表示されない、または「!」マークがある場合はドライバの再インストールを試みる

---

### 3. T5810のBIOSチップを探す

1. T5810の裏蓋を開ける
2. マザーボード上の **SOIC8（8ピン）フラッシュROMチップ** を探す
   - T5810では `W25Q128FVSQ` または `W25Q128JVSQ`（Winbond製、16MB）が搭載されている
   - 型番をメモしておく
3. チップのピン1位置（●マークや切り欠き）を確認

---

### 4. クリップの接続

1. **T5810の電源が完全にOFF・ACアダプタ抜き**であることを再確認
2. SOIC8クリップをBIOSチップにしっかり噛ませる
   - ピン1（赤線側）をチップのピン1に合わせる
3. クリップの反対側をCH341Aの **25xxソケット** に接続
   - 基板上の `25` と書かれた8ピンソケットを使用（`24` ではない）

---

### 5. BIOSの読み出し（Dump）

1. **AsProgrammer.exe を右クリック → 管理者として実行**
2. メニュー `Hardware` → `CH341a` を選択
3. ツールバーの **「Read ID」ボタン** をクリックしてチップを自動検出
   - 候補が表示されたら該当する型番をダブルクリックして選択
   - 自動検出できない場合はメニュー `IC` → `SPI` → メーカー → 型番の順に手動選択
4. ツールバーの **「Read IC」ボタン** をクリック
5. 完了したら **「Save Buffer」** でファイルを保存
   → 例：`T5810_BIOS_backup.bin`（サイズは16,777,216バイト）
6. **もう一度「Read IC」→ 別名で保存**（`T5810_BIOS_backup2.bin`）
7. 2つのファイルをコマンドプロンプトで比較して一致を確認

```cmd
fc /b T5810_BIOS_backup.bin T5810_BIOS_backup2.bin
```
`差異はありません` と出ればOK。

---

### 6. 後片付け

1. AsProgrammerを閉じる
2. クリップをチップから外す
3. T5810を元通りに組み立てる

---

### トラブルシューティング

| 症状 | 原因と対処 |
|------|-----------|
| チップが認識されない | クリップの接触不良 → 付け直す |
| Read IDで検出できない | チップ型番を `IC` メニューから手動選択する |
| ドライバが当たらない | Win11の署名強制を一時無効化して再インストール |
| 2回のDumpが一致しない | 接触不良 → クリップを付け直して再読み出し |
