## BIOSの書き込み（フラッシュ）- CH341A使用

---

### 必要なもの

- CH341AプログラマーとUSBケーブル
- SOIC8テストクリップ
- AsProgrammer v2.1.2
- 書き込む改造済みBIOSファイル（例：`T5810_BIOS_backup_mod3.bin`）
- T5810本体（電源OFF・ACアダプタ抜き）

---

### 1. 書き込み前の確認

- 書き込むファイルのサイズが **16,777,216バイト（16MB）** であることを確認する
```cmd
  dir T5810_BIOS_backup_mod3.bin
```

- サイズが異なる場合は書き込みを中止する

---

### 2. クリップの接続

1. **T5810の電源が完全にOFF・ACアダプタ抜き**であることを確認する
2. SOIC8クリップをBIOSチップにしっかり噛ませる
   - ピン1（赤線側）をチップのピン1に合わせる
3. クリップの反対側をCH341Aの **25xxソケット** に接続する
   - 基板上の `25` と書かれた8ピンソケットを使用（`24` ではない）
4. CH341AをPCのUSBポートに挿す

---

### 3. チップの接続確認

1. `AsProgrammer.exe` を**右クリック → 管理者として実行**する
2. メニュー `Hardware` → `CH341a` を選択する
3. ツールバーの **「Read ID」ボタン** をクリックしてチップを自動検出する
   - 候補が表示されたら該当する型番（例：`W25Q128FV`）をダブルクリックして選択する
   - 自動検出できない場合はメニュー `IC` → `SPI` → メーカー → 型番の順に手動選択する
   - チップが認識されない場合はクリップの接触不良 → 付け直す

---

### 4. 書き込むファイルを読み込む

1. ツールバーの **「Open File」アイコン** をクリックする
2. 書き込む改造済みBIOSファイルを選択する  
   例：`T5810_BIOS_backup_mod3.bin`
3. バッファにファイルの内容が読み込まれる

---

### 5. 書き込み保護の解除

1. ツールバーの **「Unprotect」ボタン** の右側の▼をクリックする
2. ドロップダウンから **`Set the protection bits`** を選択する
3. 画面下部のログに `New SREG:` が表示され、保護解除が完了したことを確認する

---

### 6. BIOSチップへの書き込み

1. ツールバーの **「Erase IC」ボタン** をクリックしてチップを消去する
   - 完了まで数十秒かかる
   - エラーが出た場合はクリップの接触不良 → 付け直して再試行
2. ツールバーの **「Program IC」ボタン** をクリックして書き込みを開始する
   - 完了まで数分かかる（W25Q128FVの場合、約3〜4分）
3. 書き込みが完了したらツールバーの **「Verify」ボタン** をクリックして照合する
   - `Verification success` と表示されれば書き込み成功
   - `Verification error` が表示された場合は手順5から再試行する

---

### 7. 後片付け

1. AsProgrammerを閉じる
2. CH341AをUSBから抜く
3. クリップをチップから外す
4. T5810を元通りに組み立てる

---

### 8. 書き込み後の手順

1. T5810を起動する
2. 電源投入時に `F2` を押してBIOS設定画面に入る
3. **`Restore Defaults`**（デフォルト設定を復元）を実行する
4. 変更を保存して再起動する
5. 追加の設定を調整したい場合は、再度BIOS設定画面に入る

---

### トラブルシューティング

| 症状 | 原因と対処 |
|---|---|
| Read IDでチップが認識されない | クリップの接触不良 → 付け直す |
| Erase ICでエラーが出る | 接触不良 → クリップを付け直して再試行 |
| Verification errorが出る | 手順5（Unprotect）から再試行する |
| 起動後にUEFIが正常に動作しない | LGA 2011-3の既知バグ → 下記手順で対処 |

---

### 起動後にUEFIシステムが起動しない場合

LGA 2011-3 X99プラットフォームの既知のUEFIバグ。以下の手順で対処する：

1. PCをシャットダウンする
2. 電源コードを抜く
3. **すべてのストレージデバイス（SSD/HDD）を取り外す**
4. 電源コードを繋いでPCを起動する
5. BIOS設定画面に入り `Restore Defaults` を実行する
6. 変更を保存して再起動する
7. 必要に応じてRAMタイミング等の追加設定を行う（任意）
8. PCをシャットダウンし電源コードを抜く
9. 取り外したストレージデバイスをすべて接続し直す
10. PCを起動する → 正常に動作するはず
