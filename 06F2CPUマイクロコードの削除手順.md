## 06F2 CPUマイクロコードの削除手順（MMTool Aptio V使用）

---

### 前提

- 作業前に必ずBIOSファイルのバックアップを取っておくこと
- 使用するBIOSファイルはすでにPC上に保存済みであること
- MMToolは日本語環境での表示修正済みのもの（`mmtool_a5.exe`）を使用する

---

### 手順

1. `mmtool_a5.exe` を**右クリック → 管理者として実行**する

2. **BIOSファイルを読み込む**
   - 画面左上の `[Load Image]` ボタンをクリックする
   - ファイル選択ダイアログが開くので、右下のファイル種別を `All Files (*.*)` に変更する
   - 作業対象のBIOSファイル（`.bin` など）を選択して `[開く]` をクリックする
   - 画面にBIOSの内容が読み込まれる

3. **CPU Patchタブに移動する**
   - 画面上部のタブから `[CPU Patch]` をクリックする
   - CPU IDとそのバージョン・日付の一覧テーブルが表示される

4. **CPU ID `06F2` のエントリをすべて削除する**
   - テーブルの中から `CPU ID` 列が `06F2` と表示されているエントリを探す
   - 該当エントリを**クリックして選択**する（行が青くハイライトされる）
   - 画面右側または下部の `[Delete Patch Data]` ボタンをクリックする
   - 確認ダイアログが表示されたら `[Yes]` または `[OK]` をクリックする
   - `06F2` のエントリが複数ある場合は、同じ操作を繰り返してすべて削除する
   - 削除後、テーブルに `06F2` のエントリが残っていないことを確認する

5. **変更したBIOSを保存する**
   - 画面上部の `[Save Image As]` ボタン（またはメニュー `File` → `Save Image As`）をクリックする
   - **元のファイルとは別の名前**で保存する  
     例：`T5810_BIOS_backup_mod.bin`
   - 保存が完了したらMMToolを閉じる

---

### 注意事項

- `06F2` のエントリが見当たらない場合は、読み込んだBIOSファイルが正しいか確認する
- 削除後のBIOSファイルは元ファイルより若干サイズが小さくなる場合がある（正常）
- 保存後、再度MMToolで保存したファイルを開き、`06F2` エントリが消えていることを確認してから次の作業に進むと安全
