# UbuntuでWindows 10を高速仮想化
## 1. 物理マシンの準備（BIOS/UEFI）
まず、CPUの仮想化支援機能が有効であることを確認してください。

Intel VT-x および VT-d が「Enabled」になっていること。

これがオフだと、KVMの真価（ハードウェア加速）が発揮されません。

## 2. コマンド3発で基盤構築
Ubuntuのターミナルで以下を実行してください。

```
# 1. 必要なツールをすべて導入
sudo apt update && sudo apt install -y qemu-kvm libvirt-daemon-system libvirt-clients virt-manager bridge-utils ovmf

# 2. 自分のユーザーをグループに追加（sudoなしで操作可能にする）
sudo adduser $USER libvirt
sudo adduser $USER kvm

# 3. 設定反映のため、一度ログアウトして再ログイン（必須）
# 3. 仮想化サービスを起動し、PC起動時に自動開始するよう設定
sudo systemctl enable --now libvirtd

1. 仮想マシンマネージャーの起動
ターミナルで以下を叩くか、アプリケーション一覧から「仮想マシンマネージャー」を起動してください。

Bash
virt-manager
2. 新しい仮想マシンの作成
左上の**「新しい仮想マシンを作成」**アイコン（PCの形）をクリック。

**「ローカルのインストールメディア (ISOイメージ)」**を選択して「進む」。

ISOの選択:

Windows 10のISOファイルを選択。

「このメディアからオペレーティングシステムを自動的に検出する」にチェックが入っていることを確認。

メモリとCPU: ヒデさんの潤沢なリソースを割り当てます。

メモリ: 16384 (16GB) 以上

CPU: 8 以上

ストレージ: 「この仮想マシンのディスクイメージを作成する」で容量を指定（例: 100GB〜）。

3. 【重要】インストール開始直前の「隠し設定」
ここが最も重要です。 「完了」を押す前に、必ず**「インストールの前に設定をカスタマイズする」**にチェックを入れてください。

チェックを入れて「完了」を押すと詳細設定画面が開くので、以下を反映させます。

概要: * チップセット: Q35

ファームウェア: UEFI (OVMF)

CPU: * 「ホストのCPU構成をコピーする (host-passthrough)」にチェック（これでXeonの性能をフル活用できます）。

SATAディスク 1:

ディスクバス: VirtIO に変更（爆速化のため）。

NIC (ネットワーク):

デバイスモデル: virtio に変更。

新規ハードウェアを追加 (左下のボタン):

ストレージを追加し、デバイスの種類を「CD-ROM」、バスを「SATA」にして、先ほどの virtio-win.iso を選択してください（ドライバ読み込み用）。

4. インストール開始
右上の**「インストールの開始」**をクリックします。

Windowsのセットアップが進み、「インストール場所」を選ぶ画面で何も表示されないはずです。

**「ドライバーの読み込み」**をクリックし、セットしておいた virtio-win.iso の中の amd64/w10 フォルダを選択してください。これで爆速のVirtIOディスクが認識されます。
```
## 3. Windows 10を爆速にする「4つの急所」

「4つの急所」virt-manager で仮想マシンを作成する際、以下の設定を必ず行ってください。

これだけで体感速度が劇的に変わります。

| 設定項目 | 推奨設定 | 理由 | 
| ------- | -------- | ------|
| チップセット | Q35 | 最新のPCIeバス構成を利用するため |
| ファームウェア | UEFI(OVMF) | 起動の高速化とモダンなOS管理のため |
| ディスクバス | VirtIO | **最重要。** 標準のIDE/SATAより圧倒的に速い |
| ネットワーク | virtio | ネットワーク遅延を最小化するため |

## 4. 実行時のポイント：VirtIOドライバのロード
Windows 10のインストール画面で「インストール場所（ドライブ）」が表示されないことがありますが、これは VirtIO を選択しているために起こる正常な挙動です。

```
Windows 10をインストールする際に、KVMの仮想ディスクやネットワークを認識させるために必要な正確なファイル名は以下の通りです。

1. インストール時に読み込ませるドライバ
virtio-win.iso (753M)

これを仮想マシンの「2枚目のCD-ROM」としてセットします。中にはWindows 10用のディスクドライバやネットワークドライバがすべて入っています。

2. インストール後にWindows上で実行するツール
virtio-win-guest-tools.exe (29M)
```

Windowsのインストールが無事終わった後に、このファイルをWindows内で実行してください。画面の解像度調整や、マウスの動きをスムーズにするためのエージェントが一括でインストールされます。

1. インストール開始前に、仮想マシンの設定で「もう一つのCD-ROMドライブ」を追加する。

1. そこに、あらかじめダウンロードしておいた virtio-win.iso をセットする。

1. Windowsのインストール画面で「ドライバーの読み込み」を押し、CD内の NetKVM や viostor（Windows 10用フォルダ）を選択すればドライブが認識されます。

## 5. 補足：CPUの割り当て（ヒデさんの環境向け）
CPUが大量にあるのであれば、設定画面の「CPU」項目で 「Copy host CPU configuration」 にチェックを入れてください。これにより、仮想マシン内のWindowsが「Xeon」として正しく認識し、命令セットをフル活用できるようになります。

まずは virt-manager を起動して、Windows 10のISOを読み込ませるところから始めてみてください。
途中で「ネットワークが繋がらない」「ドライブが見えない」などの詰まりどころがあれば、すぐにサポートします！
