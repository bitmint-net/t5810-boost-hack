# Dell Precision T5810: Xeon E5 v3 Turbo Boost Unlock (TBU) Guide

Dell Precision T5810（およびT7810/T7910）に搭載された **Haswell-EP (Xeon E5 v3)** シリーズの潜在能力を100%引き出し、全コアをターボブースト最大倍率で固定するための実践ガイドです。

---

### **1. ターボアンロックの仕組み**
通常、Xeon E5 v3は全コア稼働時にクロックが低下するように設計されています。この制限はCPU内の「マイクロコード」によって制御されています。
本手法では、**BIOSからマイクロコードを削除**し、OS起動時に**特定のEFIドライバ**を読み込ませることで、全コアを単一コア最大ターボ倍率（All-core Turbo）まで強制的に引き上げます。



---

### **2. 必要なツール・ファイル**
作業前に以下のツールを揃えてください。

* **UEFITool (v0.28.0)**: BIOSの編集用。
* **v3turbo.efi / V3.efi**: ターボアンロック用のEFIドライバ。
* **CH341A プログラマ & 1.8Vアダプタ**: ブリック復旧およびBIOS直接書き換え用。
* **S3TurboTool**: マイクロコードの特定とパッチ適用を支援するツール。

---

### **3. 実践ステップ**

#### **Step 1: BIOS ROMのバックアップ**
1.  **CH341A** を使用し、マザーボード上のBIOSチップから現在のROMを吸い出します。
2.  **重要**: 必ず2回以上吸い出し、バイナリ比較（ハッシュ値の確認）を行って、バックアップが完璧であることを確認してください。

#### **Step 2: マイクロコードの除去 (Microcode Removal)**
1.  **UEFITool** で吸い出したROMを開きます。
2.  `CPU Microcode` セクションを探し、Haswell-EP用のパッチ（通常は `06F2`）を特定して **Remove**（削除）します。
3.  これにより、CPUが起動時に制限付きの命令セットを読み込むのを防ぎます。

#### **Step 3: EFIドライバの注入 (Driver Injection)**
LTTのスレッドで推奨されている、最も安全で確実な「ブートローダー経由」の方法です。

1.  **rEFInd** または **OpenCore** を、小型のUSBメモリ（またはドライブの隠しパーティション）にインストールします。
2.  `EFI/BOOT/drivers` フォルダに `v3turbo.efi` を配置します。
3.  BIOSのブート順位をそのドライブに設定します。起動時にこのドライバが読み込まれることで、OS起動の瞬間にターボ倍率がアンロックされます。



---

### **4. 安定化：アンダーボルト (Undervolting)**
全コア最大クロック化は消費電力と発熱を急増させます。

* `-50mV` から `-70mV` 程度の **Voltage Offset** を適用してください。
* これにより、TDP（熱設計電力）の枠内でより高いクロックを維持でき、VRM（電源回路）への負荷を軽減できます。

---

### **⚠️ 警告・注意点**
* **電圧リスク**: T5810のBIOSチップは **1.8V動作** のものが多いため、CH341Aを使用する際は必ず **1.8Vアダプタ** を介してください。3.3Vを直結するとチップを焼く恐れがあります。
* **冷却の強化**: 全コアがフル稼働するため、標準のヒートシンクでも耐えられますが、エアフローの最適化を推奨します。
* **自己責任**: BIOSの改造はメーカー保証外であり、失敗すると起動不能（ブリック）になるリスクを伴います。必ず物理バックアップを手元に置いて作業してください。

---
*Based on discussions from Linus Tech Tips Forum.*
